# DEPRECATED — Buzz runs on AWS only (deploy/aws/buzz.tf).
#
# Production Buzz stack (replaces this file):
#   ECS Fargate relay  -> ghcr.io/block/buzz
#   RDS Postgres 16    -> Buzz schema (BUZZ_AUTO_MIGRATE)
#   ElastiCache Redis 7 -> cache (TLS + auth token)
#   S3                 -> buzz-media bucket (IAM task role, no MinIO)
#
# Apply: cd deploy/aws && terraform apply
# Outputs: buzz_relay_internal_url, buzz_relay_public_url (when buzz_hostname set)
#
# For local Vital development without Buzz, use the HMAC talk surface
# (src/talk/surface.ts) or point BUZZ_RELAY_URL at a deployed relay.
